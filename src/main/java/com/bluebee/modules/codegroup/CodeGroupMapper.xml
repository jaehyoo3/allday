<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bluebee.modules.codegroup.CodeGroupMapper">

    <resultMap id="resultMapObj" type="com.bluebee.modules.codegroup.CodeGroup"></resultMap>
	
	<select id="selectList" resultMap="resultMapObj">
		SELECT
			a.ccgSeq
			,a.codeName
			,a.userNY
			,a.regDate
			,a.modifyDate
            ,(SELECT count(bb.ccSeq) FROM code bb WHERE bb.codegroup_ccgSeq = a.ccgSeq) as cnt
			FROM codegroup a
			WHERE 1=1
				AND a.codeName LIKE CONCAT('%', #{shValue}, '%')

	</select>
</mapper>		
